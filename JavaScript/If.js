let idade = Math.floor(Math.random() * 100) + 1; // Gera uma idade aleatória entre 1 e 100

if (idade >= 18) {
    console.log('Maior de idade!' + idade + ' Anos');
} 
else {
    console.log('Menor de idade!' + idade);
}